import 'package:flutter/material.dart';

class StrategyMethodPage extends StatelessWidget {
  const StrategyMethodPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}